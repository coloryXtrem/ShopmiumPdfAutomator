<Window x:Class="ShopmiumPdfAutomator.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mise à jour prête"
        Width="460" Height="240"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0D0D0D"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="BtnRestart" TargetType="Button">
            <Setter Property="Background"    Value="#00CC66"/>
            <Setter Property="Foreground"    Value="White"/>
            <Setter Property="FontFamily"    Value="Consolas"/>
            <Setter Property="FontSize"      Value="12"/>
            <Setter Property="FontWeight"    Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"        Value="Hand"/>
            <Setter Property="Height"        Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="20,0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#009944"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="BtnLater" TargetType="Button">
            <Setter Property="Background"    Value="Transparent"/>
            <Setter Property="Foreground"    Value="#555"/>
            <Setter Property="FontFamily"    Value="Consolas"/>
            <Setter Property="FontSize"      Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"        Value="Hand"/>
            <Setter Property="Height"        Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="12,0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#00CC66" BorderThickness="1" CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="#001A0D"
                    CornerRadius="6,6,0,0"
                    Padding="20,14">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="✅  MISE À JOUR PRÊTE"
                                   FontFamily="Consolas" FontSize="13"
                                   FontWeight="Bold" Foreground="#00CC66"/>
                        <TextBlock x:Name="LblVersions"
                                   FontFamily="Consolas" FontSize="10"
                                   Foreground="#666" Margin="0,3,0,0"/>
                    </StackPanel>
                    <Button x:Name="BtnClose"
                            Content="✕"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Background="Transparent"
                            Foreground="#333"
                            BorderThickness="0"
                            FontSize="14"
                            Cursor="Hand"
                            Visibility="{Binding CloseVisible}"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- Corps -->
            <StackPanel Grid.Row="1" Margin="20,14,20,8">
                <TextBlock Text="NOUVEAUTÉS" FontFamily="Consolas"
                           FontSize="9" Foreground="#444" Margin="0,0,0,5"/>
                <ScrollViewer MaxHeight="80" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="LblNotes"
                               FontFamily="Consolas" FontSize="10"
                               Foreground="#AAA" TextWrapping="Wrap"/>
                </ScrollViewer>
            </StackPanel>

            <!-- Boutons -->
            <Grid Grid.Row="2" Margin="20,4,20,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BtnSkip"
                        Grid.Column="1"
                        Content="Plus tard"
                        Style="{StaticResource BtnLater}"
                        Width="90"
                        Margin="0,0,8,0"
                        Click="BtnSkip_Click"/>
                <Button x:Name="BtnRestart"
                        Grid.Column="2"
                        Content="🔄  Redémarrer l'app"
                        Style="{StaticResource BtnRestart}"
                        Width="180"
                        Click="BtnRestart_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
